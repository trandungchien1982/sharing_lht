
Hãy giải nén file .gradle_staging.zip trước khi chạy nhé.

# Create Spring Boot + Gradle, running in JDK8
# The Gradle folder will be ./.gradle_staging (contains all downloaded JAR files)

When finish starting, we can access into :
	+ Staging : 	http://localhost:9000
					http://localhost:9000/welcome
					http://localhost:9000/dashboard